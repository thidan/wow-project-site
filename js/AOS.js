AOS.init({
  // duração da animação (em ms)
  duration: 800,

  // atraso antes de iniciar a animação (em ms)
  delay: 0,

  // timing function (como no CSS) para suavizar a animação
  easing: 'ease',

});